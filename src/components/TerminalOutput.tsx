import React from 'react';
import { View, Text } from 'react-native';

export const TerminalOutput = () => (
  <View style={{ backgroundColor: '#000', padding: 8 }}>
    <Text style={{ color: '#00D4FF' }}>Connection closed (Legacy Module)</Text>
  </View>
);